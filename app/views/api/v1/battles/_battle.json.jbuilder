json.merge! battle.api_expose

# json.extract! battle, :id, :room_id, :max_survivors, :time_limit, :start_time, :end_time

# json.winner battle.winner, partial: 'api/v1/users/user', as: :user

# json.challenge battle, partial: 'api/v1/battles/challenge', as: :battle

# json.players battle.players, partial: 'api/v1/users/user', as: :user
