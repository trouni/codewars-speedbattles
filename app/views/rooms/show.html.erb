<% content_for :title, @room.name %>

<room
  :announcer-window="slotProps.announcerWindow"
  :battle-joinable="slotProps.battleJoinable"
  :battle-loading="slotProps.battleLoading"
  :battle-stage="slotProps.battleStage"
  :battle="slotProps.battle"
  :chat="slotProps.chat"
  :countdown="slotProps.countdown"
  :current-user-is-moderator="slotProps.currentUserIsModerator"
  :current-user="slotProps.currentUser"
  :focus="slotProps.focus"
  :initializing="slotProps.initializing"
  :messages-loading="slotProps.messagesLoading"
  :ready-to-start="slotProps.readyToStart"
  :room-name="slotProps.roomName"
  :room-players-loading="slotProps.roomPlayersLoading"
  :room-players="slotProps.roomPlayers"
  :room-settings-initialized="slotProps.roomSettingsInitialized"
  :room="slotProps.room"
  :settings="slotProps.settings"
  :signed-in="slotProps.signedIn"
  :users-loading="slotProps.usersLoading"
  :users="slotProps.users">
</room>